{
  "name": "member-club",
  "author": "Fellipe Silvestre",
  "description": "Aplicação web para simular cartão fidelidade de salão de cabeleireiro",
  "scripts": {
    "server": "json-server --watch db.json --port 3333"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.1"
  }
}
